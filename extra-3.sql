-- What is the maximum, minimum, and average age of the people in this database? (1 pt)

SELECT MAX(age) FROM persons;
-- The maximum age is 110 years old.

SELECT MIN(age) FROM persons;
-- The maximum age is 18 years old.

